export enum ENVOIRMENT {
  DEVELOPMENT = 'dev',
  PRODUCTION = 'prod',
  TEST = 'test',
  STAGING = 'stg',
}

export enum LOG_LEVEL {
  FATAL = 'fatal',
  ERROR = 'error',
  WARN = 'warn',
  INFO = 'info',
  DEBUG = 'debug',
  TRACE = 'trace',
}
